# Required when exposed beyond localhost. Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
REMOTE_TOKEN=change-me
PORT=4174
HOST=127.0.0.1
# CODEX_BIN=codex
# DATA_DIR=.data
# CODEX_DECK_CLOUDFLARED=cloudflared
# CODEX_DECK_TUNNEL_PROTOCOL=http2
